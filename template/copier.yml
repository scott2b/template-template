# template/copier.yml
_answers_file: copier-answers.yml

project_name:
  type: str
  help: Human-readable project name
  default: "Example Project"

project_slug:
  type: str
  help: Slug used for directories, package names, etc.
  default: "{{ project_name|lower|replace(' ', '-') }}"
